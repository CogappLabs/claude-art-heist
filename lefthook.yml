pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,css,json}"
      run: npx biome check --no-errors-on-unmatched {staged_files}
    typecheck:
      glob: "*.ts"
      run: npx tsc --noEmit
